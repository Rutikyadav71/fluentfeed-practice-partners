# Server
PORT=5000
MONGODB_URI=mongodb://localhost:27017/fluentfeed
CLIENT_URL=http://localhost:5173

# development | production
# In production, MONGODB_URI and CLIENT_URL become required with no
# fallback — the server will refuse to start without them set explicitly.
NODE_ENV=development
